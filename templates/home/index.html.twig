{% extends 'base.html.twig' %}

{% block title %}{{ "home_page.home"|trans }}{% endblock %}

{% block body %}
    {% if app.user %}
        <div class="mb-3">
            {{ "home_page.logged_in_as"|trans }} {{ app.user.username }}, <a href="{{ path('app_logout') }}">{{ "home_page.logout"|trans }}</a>
        </div>
    {% endif %}
    <div>
        <h1>{{ "home_page.recipes"|trans }}</h1>
        <p class="lead">{{ "home_page.text"|trans }}</p>
        <ul>
            {% for recipeName in recipeNames %}
                <li>
                    <div>
                        {{ recipeName.title }}
                        {{ recipeName.description }}
                        <ul>
                            {% for recipe in recipes %}
                                {% if recipeName.title == recipe.recipe.title %}
                                    <li>
                                        {{ recipe.ingredient.title }}
                                        {{ recipe.amount }}
                                        {{ recipe.measure.title }}
                                    </li>
                                {% endif %}
                            {% endfor %}
                        </ul>
                    </div>
                </li>
            {% endfor %}
        </ul>
    </div>

{% endblock %}